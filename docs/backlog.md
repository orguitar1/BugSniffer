# BugSniffer Backlog

- Migrate Finding model from Pydantic V2 class-based config to ConfigDict (use_enum_values=True)
- Split requirements.txt into requirements.txt and requirements-dev.txt (pytest and httpx are dev-only dependencies)
